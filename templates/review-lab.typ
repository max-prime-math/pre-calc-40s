#import "_precalc-shell.typ": *

#show: shell.with(
  flow: "Review/Lab",
  title: "[unit review]",
  unit: "[unit]",
)

#callout("Purpose", [
  This is not a worksheet day. It is a structured review day with retrieval, mixed practice, teacher observation, and error repair.
])

= Retrieval Grid

#three-col(
  small-box("Definition/property"),
  small-box("Procedure"),
  small-box("Graph/interpretation"),
)

= Unit Map

Major ideas from this unit:

#write-box(height: 0.65in)

= Mixed Practice

#two-col(
  small-box("Core fluency", height: 1.15in),
  small-box("Method choice", height: 1.15in),
)

= Error Repair

Error:

#write-box(height: 0.5in)

Correct version:

#write-box(height: 0.65in)

= Test Readiness

I am ready if I can:

#write-box(height: 0.55in)
