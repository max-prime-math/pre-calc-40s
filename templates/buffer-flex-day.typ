#import "_precalc-shell.typ": *

#show: shell.with(
  flow: "Buffer / Flex Day",
  title: "[buffer focus]",
  unit: "[unit]",
)

#callout("Purpose", [
  Use only when a calendar buffer becomes an instructional day. Give the day a clear job instead of defaulting to vague catch-up.
])

= Today's Job

Circle one: prerequisite repair / fluency / mixed practice / error analysis / retrieval / extension

#write-box(height: 0.35in)

= Core Task

#write-box(height: 1.3in)

= Teacher Checkpoint

What evidence shows I am ready to move on?

#write-box(height: 0.5in)

= Extension Or Repair

#two-col(
  small-box("Repair path", height: 0.9in),
  small-box("Extension path", height: 0.9in),
)
