#import "_precalc-shell.typ": *

#show: shell.with(
  flow: "Practice Exam / Mock Review",
  title: "[practice exam focus]",
  unit: "Cumulative",
)

#callout("Purpose", [
  Use for mock quizzes, practice exams, and mock provincial days. The important learning happens in the analysis after the timed set.
])

= Before The Timed Set

Strategy reminder:

#write-box(height: 0.45in)

High-risk topics:

#write-box(height: 0.45in)

= Timed Set

Time limit:

#write-box(height: 0.3in)

Questions included:

#write-box(height: 0.35in)

= Error Analysis

#three-col(
  small-box("I knew it"),
  small-box("I almost knew it"),
  small-box("I need reteach"),
)

= Repair Block

Problem type to repair:

#write-box(height: 0.45in)

Corrected example:

#write-box(height: 0.9in)

= Next Retrieval

#write-box(height: 0.4in)
