#import "_precalc-shell.typ": *

#show: shell.with(
  flow: "Repair/Lab",
  title: "[assessment repair]",
  unit: "[unit]",
)

#callout("Purpose", [
  Use after a quiz or test. The goal is to diagnose error types and reteach high-leverage gaps before moving on.
])

= Error Categories

#three-col(
  small-box("Algebra/procedure"),
  small-box("Concept/meaning"),
  small-box("Notation/attention"),
)

= Class Repair Example

Original problem:

#write-box(height: 0.45in)

Where the error happens:

#write-box(height: 0.5in)

Corrected solution:

#write-box(height: 0.9in)

= My Repair

Problem I missed or need to strengthen:

#write-box(height: 0.45in)

Corrected thinking:

#write-box(height: 0.8in)

= Retrieval Commitment

This will come back on:

#write-box(height: 0.3in)
