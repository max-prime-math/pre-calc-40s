#import "_precalc-shell.typ": *

#show: shell.with(
  flow: "Review",
  title: "[review focus]",
  unit: "[unit or course]",
)

#callout("Purpose", [
  Review should be cumulative and active. Students should choose methods, explain errors, and retrieve without being over-cued.
])

= Retrieval Warm-Up

#three-col(
  small-box("Functions"),
  small-box("Algebra"),
  small-box("Trigonometry or exp/log"),
)

= Review Focus

Today's focus:

#write-box(height: 0.4in)

Why this matters:

#write-box(height: 0.35in)

= Mixed Problems

#write-box(height: 1.45in)

= Method Choice

How do I know which method to use?

#write-box(height: 0.65in)

= One Error To Avoid

#write-box(height: 0.55in)

= Exit Check

#write-box(height: 0.45in)
