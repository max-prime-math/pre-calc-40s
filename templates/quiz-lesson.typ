#import "_precalc-shell.typ": *

#show: shell.with(
  flow: "Quiz + Lesson",
  title: "[quiz + new lesson]",
  unit: "[unit]",
)

#callout("Purpose", [
  Use when a short quiz must share the period with new acquisition. Keep the quiz brief and the new lesson narrow.
])

= Quiz

Quiz topic:

#write-box(height: 0.35in)

What this quiz is checking:

#write-box(height: 0.35in)

= Bridge

The quiz connects to today's new idea because:

#write-box(height: 0.45in)

= New Target

#write-box(height: 0.4in)

= I Do

Clean worked example:

#write-box(height: 1.05in)

= We Do

#two-col(
  small-box("Guided example 1", height: 0.9in),
  small-box("Guided example 2", height: 0.9in),
)

= Exit Ticket

#write-box(height: 0.5in)
