#import "_precalc-shell.typ": *

#show: shell.with(
  flow: "Quiz + Lab",
  title: "[quiz topic]",
  unit: "[unit]",
)

#callout("Purpose", [
  Short quiz for evidence, then a lab that repairs errors and extends practice. The quiz should inform the rest of the class.
])

= Pre-Quiz Retrieval

#three-col(
  small-box("Fact/identity/rule"),
  small-box("Worked mini-problem"),
  small-box("Common trap"),
)

= Quiz

Quiz topic:

#write-box(height: 0.35in)

After the quiz, circle the best description:

Accurate and fluent / Accurate but slow / Some gaps / Need reteach

= Lab After Quiz

Main error pattern from the quiz:

#write-box(height: 0.45in)

Teacher repair example:

#write-box(height: 0.85in)

Practice set:

#write-box(height: 0.9in)

= Reflection

One thing I need to retrieve again:

#write-box(height: 0.45in)
