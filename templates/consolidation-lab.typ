#import "_precalc-shell.typ": *

#show: shell.with(
  flow: "Consolidation Lab",
  title: "[skill cluster]",
  unit: "[unit]",
)

#callout("Purpose", [
  Use this for structured practice, mixed examples, and teacher observation. It is not vague catch-up time.
])

= Retrieval Warm-Up

#three-col(
  small-box("Algebra fluency"),
  small-box("Graph/notation"),
  small-box("Current unit"),
)

= Lab Focus

The skill we are strengthening:

#write-box(height: 0.4in)

The decision we need to practise:

#write-box(height: 0.4in)

= Part A: Fluency

#write-box(height: 1.05in)

= Part B: Mixed Practice

#write-box(height: 1.15in)

= Part C: Error Analysis

Wrong solution or common mistake:

#write-box(height: 0.75in)

Corrected thinking:

#write-box(height: 0.6in)

= Exit Check

#write-box(height: 0.45in)
